---
interface Props {
	isCopyright?: boolean;
}

const { isCopyright = true } = Astro.props;

const copyrightText = `© 2024-${new Date().getFullYear()} Jere L`;
---

<footer class="main-footer">
	{
    isCopyright && ( <hacker-text>{copyrightText}</hacker-text> )
	}
</footer>

<style>
	footer {
		position: fixed;
		left: 0;
		bottom: 0.8rem;
		width: 100%;
		text-align: center;
	}
</style>

<script>
  import hackerText from '@/lib/hacker-text';

  let value: string | undefined;

  class HackerText extends HTMLElement {
    connectedCallback() {
      const text = value ??= this?.innerText;
      this?.addEventListener('mouseover', () => {
          hackerText(this, text);
      })
    }
  }

  customElements.define('hacker-text', HackerText);
</script>
